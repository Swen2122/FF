using UnityEngine;

public class EnemySkillManager : CharacterSkillManager
{
    void Update()
    {
       /* Vector2 shoterPosition = gameObject.transform.position;
        Vector2 targetPosition = shoterPosition + Vector2.left;
        (skillBindings[0].skill as TargetedSkill).TryUseSkillAtPositionAI(targetPosition); */
    }
}
